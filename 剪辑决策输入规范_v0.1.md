# 剪辑决策输入规范 v0.1

## 1. 目的

本规范定义“剪辑决策模型”接收的数据。模型只判断哪些内容应该删除、保留或交由人工复核，不直接生成 FCP7 XML，也不自行计算最终剪辑帧。

## 2. 输入组成

一次完整输入由四部分组成：

1. 素材信息。
2. 带定位信息的转录文本。
3. 可选的声音与画面事件。
4. 本次任务的剪辑配置。

## 3. 素材信息

必填字段：

```json
{
  "media_id": "video_001",
  "filename": "测试.mp4",
  "fps": 50,
  "total_frames": 3796,
  "duration_seconds": 75.92,
  "start_timecode": "00:00:00:00",
  "language": "zh-CN",
  "content_type": "single_speaker_talking_head"
}
```

规则：

- `fps`、`total_frames` 和 `duration_seconds` 必须由媒体探测程序读取，不能由模型猜测。
- 所有时间都以原始素材为参照，不能使用波纹删除后的时间线时间。
- 同一任务中的所有输入必须使用同一素材版本。

## 4. 转录文本

### 4.1 最低要求

转录必须包含稳定的词语编号。模型使用词语编号指出内容范围，不直接猜测精确帧号。

```json
{
  "transcript": [
    {
      "segment_id": "s001",
      "speaker": "speaker_1",
      "text": "大家好今天我们来讲一下这个动作",
      "words": [
        {
          "word_id": "w0001",
          "text": "大家好",
          "start_seconds": 14.84,
          "end_seconds": 15.18
        },
        {
          "word_id": "w0002",
          "text": "今天",
          "start_seconds": 15.22,
          "end_seconds": 15.48
        }
      ]
    }
  ]
}
```

### 4.2 转录规则

- `word_id` 在单条素材内必须唯一且顺序稳定。
- 保留口误、重复、语气词和未完成句，不得在转录阶段先行润色。
- 无法确认的词使用原始识别结果并标记低置信度。
- 长停顿不能伪造成标点，应作为声音事件单独提供。
- 转录文本用于内容判断，不能直接替代媒体边界校准。

## 5. 声音事件

声音事件为可选但推荐输入：

```json
{
  "audio_events": [
    {
      "event_id": "a001",
      "type": "silence",
      "start_seconds": 22.52,
      "end_seconds": 23.14,
      "duration_seconds": 0.62
    },
    {
      "event_id": "a002",
      "type": "noise",
      "start_seconds": 31.08,
      "end_seconds": 31.56
    }
  ]
}
```

允许的初始类型：

- `silence`：静音或近似静音。
- `breath`：明显呼吸。
- `noise`：环境或设备噪声。
- `clap`：拍手或同步信号。
- `interruption`：外界中断。

声音事件只能辅助判断，不能仅凭时长自动决定删除。

## 6. 画面事件

第一版可不提供。将来可以加入：

```json
{
  "visual_events": [
    {
      "event_id": "v001",
      "type": "speaker_not_ready",
      "start_seconds": 0,
      "end_seconds": 6.4,
      "description": "人物整理衣服，尚未开始讲话"
    }
  ]
}
```

## 7. 剪辑配置

```json
{
  "editing_config": {
    "sop_version": "talking-head-v0.1",
    "style": "natural_tight",
    "uncertain_action": "review",
    "preserve_emotion": true,
    "remove_filler_words": "conservative",
    "allow_topic_removal": false
  }
}
```

第一版默认值：

- 风格为自然偏紧凑。
- 无法确定时输出 `review`。
- 保守处理语气词。
- 不自动删除疑似跑题内容，除非证据非常明确。

## 8. 输入完整性检查

进入模型前必须确认：

- 素材帧率和总帧数有效。
- 转录词语编号无重复、无倒序。
- 时间戳不超出素材时长。
- 所有区间满足开始时间小于结束时间。
- 输入使用的 SOP 版本存在。

输入不完整时停止决策并返回错误，不允许模型补猜缺失数据。

