# 单人口播自动粗剪 SOP v0.1

## 一、目标

在不改变原意、不损害表达完整性的前提下，自动识别并删除单人口播中的无效内容，生成精确到帧的删除清单。

本阶段只负责决定“删什么”，不处理 B-roll、字幕、转场、调色和配乐。

## 二、基本原则

1. 未标记删除的内容默认保留。
2. 不确定时保留，不进行冒险删除。
3. 删除后不得改变原意或事实关系。
4. 删除后句子必须语义完整、听感自然。
5. 内容正确性优先于节奏紧凑度。
6. AI 负责内容判断，时间线引擎负责边界校准和帧换算。

## 三、决策优先级

按以下顺序判断：

1. 是否属于无效素材。
2. 是否存在明确口误或废弃表达。
3. 是否属于重复表达。
4. 是否偏离当前主题。
5. 是否只是停顿、呼吸或语气词。
6. 删除后是否影响语义、情绪和自然度。
7. 无法确定时保留并标记人工复核。

## 四、默认删除规则

### 1. 开拍前和结束后废片

删除以下内容：

- 正式表达开始前的准备过程。
- 调整设备、姿势、衣服或状态。
- 与工作人员的无关交流。
- 正式内容结束后的等待和无效动作。

### 2. 明确口误与重新表达

当说话者明确否定、纠正或重新说一遍时：

- 删除错误或放弃的版本。
- 保留最后一次正确、完整、自然的版本。
- 如果前后版本各自包含独有信息，则不得直接删除。

### 3. 完全重复

满足以下条件时可以删除：

- 两次表达的核心信息相同。
- 后一次没有增加新的信息。
- 删除其中一次不会破坏上下文。
- 优先保留准确、完整、流畅、情绪自然的一次。

### 4. 无意义中断

可以删除：

- 被电话、噪声或现场事件打断的无效内容。
- 明确准备重新开始的半句话。
- 与视频主题无关的临时交流。
- 拍摄过程中的技术指令。

### 5. 跑题内容

只有在明显满足以下条件时删除：

- 与当前主题没有必要联系。
- 删除后上下文仍然完整。
- 不包含人物观点、情绪或有价值的补充信息。

无法确定是否跑题时默认保留。

### 6. 无效停顿

可以处理：

- 明显忘词或寻找措辞造成的长时间冷场。
- 重新表达前已经被放弃的停顿。
- 没有情绪、强调或节奏作用的空白。

不得仅根据停顿时长自动删除。

#### 句间停顿硬规则

- 两句完整话之间的间隔最多保留 `15帧`。
- 原始句间间隔大于 `15帧` 时，必须建立剪辑点，将间隔压缩至 `15帧`。
- 原始句间间隔小于或等于 `15帧` 时，不因本规则进行处理。
- 该规则是“压缩到15帧”，不是删除全部停顿；仍需保留自然换气和句间边界。
- 只有确认属于两句完整话之间的间隔时才应用；句内停顿、强调停顿、情绪停顿和话题转换停顿仍按原有语义规则判断。
- 第二环节必须利用前一句最后一个词和后一句第一个词的安全音频边界执行，禁止截断音节或呼吸尾音。

### 7. 语气词

“嗯、啊、那个、就是”等语气词仅在以下情况下删除：

- 明显影响理解或节奏。
- 删除后语音连接仍然自然。
- 不承担思考、情绪或语气功能。

第一版采取保守策略，不批量删除所有语气词。

## 五、必须保留的情况

以下内容默认保留：

- 正常呼吸和自然换气。
- 强调观点前后的停顿。
- 情绪变化和人物反应。
- 虽有重复但增加了新信息的表达。
- 用于解释、举例或加强理解的内容。
- 对建立人物可信度有价值的自然表达。
- 删除后可能产生语义歧义的内容。
- AI 无法明确判断的内容。

## 六、删除边界规则

1. AI 先确定需要删除的语言内容。
2. 起点放在被删除内容第一个有效音节之前。
3. 终点放在被删除内容最后一个有效音节之后。
4. 尽量利用自然停顿、呼吸间隙或静音区作为边界。
5. 不截断音节、辅音、呼吸尾音或下一句话开头。
6. 边界最终吸附到素材合法帧。
7. 删除区间统一采用 `[start_frame, end_frame)`。
8. 重叠或相邻的删除区间由时间线引擎统一合并。

## 七、决策结果

每个判断分为三种状态：

- `delete`：证据明确，可以自动删除。
- `keep`：应当保留。
- `review`：无法确定，需要人工确认。

只有 `delete` 会进入最终删除清单。

## 八、标准输出

```json
{
  "sop_version": "talking-head-v0.1",
  "editing_style": "natural_tight",
  "decisions": [
    {
      "action": "delete",
      "category": "口误重说",
      "content": "需要删除的文字内容",
      "reason": "第一遍表达错误，随后完整重说",
      "confidence": 0.97
    },
    {
      "action": "review",
      "category": "疑似重复",
      "content": "待确认的文字内容",
      "reason": "内容相似，但可能包含补充信息",
      "confidence": 0.63
    }
  ]
}
```

此阶段优先使用输入规范中的 `word_id` 输出文字范围。后续由逐词时间戳和边界校准器将词语范围转换成精确帧号。

具体接口以《剪辑决策输入规范 v0.1》和《剪辑决策输出规范 v0.1》为准。

## 九、人工反馈记录

每次人工修改至少记录：

- AI 原始判断。
- 人工最终判断。
- 修改原因。
- 所属规则类别。
- 是否建议更新 SOP。
- 规则适用范围：通用、视频类型、个人偏好或单次案例。

单个案例不能直接修改通用 SOP。相同问题重复出现后，再经过人工确认合并到新版本。

## 十、第一版验收标准

- 不误删关键信息。
- 不改变原意。
- 能识别明确口误、重说和完全重复。
- 删除后语言基本自然。
- 每个删除决定都有可理解的原因。
- 所有不确定内容进入人工复核。
- 最终删除边界可转换为精确帧并生成 FCP7 XML。
