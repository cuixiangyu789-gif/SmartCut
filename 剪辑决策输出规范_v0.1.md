# 剪辑决策输出规范 v0.1

## 1. 目的

本规范定义剪辑决策模型与时间线引擎之间的接口。模型输出内容判断，时间线引擎负责定位媒体边界、吸附到帧、合并区间并生成 FCP7 XML。

## 2. 输出原则

- 模型只能输出规定的 JSON 结构。
- 模型不能生成 XML。
- 模型不能虚构不存在的词语编号、时间码或帧号。
- 未输出为 `delete` 的内容默认保留。
- `review` 不得自动进入删除清单。

## 3. 顶层结构

```json
{
  "schema_version": "edit-decisions-v0.1",
  "sop_version": "talking-head-v0.1",
  "media_id": "video_001",
  "summary": {
    "delete_count": 1,
    "review_count": 1
  },
  "decisions": []
}
```

## 4. 决策对象

```json
{
  "decision_id": "d001",
  "action": "delete",
  "category": "mistake_retake",
  "start_word_id": "w0042",
  "end_word_id": "w0057",
  "content": "这个动作主要训练肩膀，不对……",
  "reason": "第一遍出现事实性口误，随后完整重说并纠正",
  "confidence": 0.97,
  "evidence": [
    "说话者使用了“不对”进行自我纠正",
    "后续版本语义完整且事实正确"
  ]
}
```

`end_word_id` 表示范围中最后一个被处理的词语编号。时间线引擎根据该词的结束时间建立右边界。

句间停顿压缩使用 `inter_word_gap` 定位，并增加目标剩余帧数：

```json
{
  "action": "delete",
  "category": "sentence_gap_compression",
  "location": {
    "type": "inter_word_gap",
    "after_word_id": "w0077",
    "before_word_id": "w0078",
    "target_remaining_frames": 15
  }
}
```

当原始间隔不超过目标帧数时，时间线引擎不得生成删除区间。

## 5. action 枚举

- `delete`：证据充分，可以自动删除。
- `keep`：容易被误删但应明确保留的内容。
- `review`：存在合理疑问，需要人工确认。

通常不需要为所有正常内容输出 `keep`。只记录具有代表性或容易误判的保留案例。

## 6. category 枚举

第一版允许以下类别：

- `pre_roll`：正式内容开始前的废片。
- `post_roll`：正式内容结束后的废片。
- `mistake`：明确口误。
- `mistake_retake`：口误后重新表达。
- `abandoned_sentence`：被放弃的半句话。
- `exact_repetition`：没有新增信息的完全重复。
- `interruption`：拍摄或环境中断。
- `technical_direction`：拍摄现场技术指令。
- `irrelevant_exchange`：与内容无关的交流。
- `empty_pause`：没有表达功能的停顿。
- `sentence_gap_compression`：两句完整话之间超过15帧的间隔，压缩后最多保留15帧。
- `filler_word`：可安全删除的语气词。
- `possible_tangent`：疑似跑题，默认进入复核。
- `protected_pause`：有强调或情绪作用的停顿，应保留。
- `other`：无法归入现有分类，必须给出详细原因。

## 7. 置信度规则

- `0.90–1.00`：证据明确，可考虑自动执行。
- `0.70–0.89`：通常进入人工复核。
- `0.00–0.69`：不得自动删除。

置信度不能代替规则判断。即使置信度较高，只要涉及可能改变原意，也必须输出 `review`。

## 8. 时间线引擎生成字段

以下字段不由模型生成，而由后续程序补充：

```json
{
  "start_frame": 949,
  "end_frame": 1054,
  "start_timecode": "00:00:18:49",
  "end_timecode": "00:00:21:04",
  "boundary_method": "word_timestamp_plus_silence_snap"
}
```

转换规则：

- 删除区间采用 `[start_frame, end_frame)`。
- 起止时间必须吸附到素材合法帧。
- 重叠和相邻删除区间在转换后合并。
- 所有计算使用整数帧，禁止用浮点秒直接写入 XML。

## 9. 完整输出示例

```json
{
  "schema_version": "edit-decisions-v0.1",
  "sop_version": "talking-head-v0.1",
  "media_id": "video_001",
  "summary": {
    "delete_count": 1,
    "review_count": 1
  },
  "decisions": [
    {
      "decision_id": "d001",
      "action": "delete",
      "category": "mistake_retake",
      "start_word_id": "w0042",
      "end_word_id": "w0057",
      "content": "错误并被重新表达的内容",
      "reason": "第一遍表达错误，随后完整重说",
      "confidence": 0.97,
      "evidence": ["存在明确自我纠正", "后一句完整替代前一句"]
    },
    {
      "decision_id": "d002",
      "action": "review",
      "category": "possible_tangent",
      "start_word_id": "w0090",
      "end_word_id": "w0112",
      "content": "疑似偏离主题的补充内容",
      "reason": "与主线关系较弱，但可能包含有价值的信息",
      "confidence": 0.64,
      "evidence": ["主题相关度较低", "仍存在补充解释作用"]
    }
  ]
}
```

## 10. 输出校验

输出进入下一环节前必须通过：

- JSON 可解析。
- 枚举值合法。
- 所有词语编号存在。
- 起始词不晚于结束词。
- `reason` 和 `evidence` 非空。
- `delete` 决策满足自动执行阈值和安全规则。
- 决策范围不存在未经解释的冲突。
