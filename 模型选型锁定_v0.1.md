# 自动剪辑模型选型锁定 v0.1

## 最终选型

自动剪辑流程采用“豆包负责转录、Codex负责剪辑决策”的固定组合，不设计运行时模型供应商切换。

### 第一环节：语音识别

- 服务：豆包语音识别大模型。
- 职责：中文口播转录、智能分句、字/词时间戳和识别置信信息。
- 输入：从原视频提取的标准化音频，或豆包接口支持的视频格式。
- 输出：项目统一的带 `word_id` 转录结构。

### 第一环节：剪辑决策

- 服务：当前项目中的 Codex 大语言模型。
- 职责：根据 SOP、逐字稿和声音事件输出 `delete`、`keep`、`review`。
- 输出：符合 `edit-decisions-v0.1` 的 JSON。

### 本地确定性处理

以下工作不交给模型：

- 音频提取和标准化。
- 输入、输出结构校验。
- 重叠决策合并和冲突处理。
- 词语时间戳向整数帧转换。
- 删除边界校准。
- FCP7 XML 生成。
- 评测和反馈记录。

## 固定原则

1. 正式转录不得自动切换到 Whisper、DeepSeek 或其他 ASR 模型。
2. 豆包语音识别不可用时任务失败并报告原因，不使用其他模型静默降级。
3. 豆包 ASR 版本和剪辑决策模型版本必须随每次运行记录。
4. 升级豆包模型版本前必须通过固定案例回归测试。
5. ASR 原始结果必须留档，不能只保存润色后的文本。
6. 模型不得直接生成 FCP7 XML。

## 接入前置条件

- 火山引擎账户。
- 已开通豆包语音识别服务。
- 豆包语音识别对应的资源标识和鉴权凭证。
- 可用的当前项目 Codex 模型。
- 明确的素材上传与数据保存政策。

凭证只能通过环境变量或本机安全凭证存储提供，不写入项目文件。
