# 剪辑决策评测与反馈规范 v0.1

## 1. 目的

本规范用于比较 AI 剪辑决策与人工结果，记录错误，并控制 SOP 的持续迭代。评测重点是“删对内容”，边界精度由后续时间线引擎单独评测。

## 2. 评测对象

每次评测至少保存：

- 原始素材版本。
- 使用的转录版本。
- SOP、提示词和输出规范版本。
- AI 原始决策 JSON。
- 人工最终决定。
- 转换后的删除帧区间。
- 导出的 FCP7 XML。

## 3. 决策结果分类

### 正确删除

AI 标记删除，人工也认为应该删除，且删除原因基本一致。

### 误删

AI 标记删除，但人工认为应该保留。

误删是第一版最严重的问题，优先级高于漏删。

### 漏删

人工认为应该删除，但 AI 没有识别，或错误地标记为保留。

### 正确复核

AI 无法确定并输出 `review`，人工确认该位置确实具有合理争议。

### 过度复核

规则已经足够明确，但 AI 仍然大量输出 `review`，导致自动化程度过低。

### 分类或理由错误

删除动作正确，但类别或解释不准确。这类错误不一定影响时间线，但会影响后续学习。

### 边界错误

内容判断正确，但最终帧边界过早、过晚、截断音节或包含不应删除的声音。该问题归入边界校准器，不直接修改内容 SOP。

## 4. 核心指标

### 删除准确率

```text
正确删除数 ÷ AI 自动删除总数
```

### 删除召回率

```text
正确删除数 ÷ 人工应删除总数
```

### 误删率

```text
误删数 ÷ AI 自动删除总数
```

### 复核率

```text
review 数 ÷ 全部候选决策数
```

### 帧边界一致率

```text
与人工边界完全相同的边界数 ÷ 全部已确认边界数
```

第一版优先优化顺序：

1. 降低误删。
2. 保证不改变原意。
3. 提高明确口误和重说的召回率。
4. 提高帧边界准确度。
5. 在安全前提下降低人工复核量。

## 5. 人工反馈记录格式

```json
{
  "feedback_id": "f001",
  "media_id": "video_001",
  "decision_id": "d001",
  "sop_version": "talking-head-v0.1",
  "ai_action": "delete",
  "human_action": "keep",
  "error_type": "false_delete",
  "category": "protected_pause",
  "human_reason": "这是结论前的强调停顿，承担节奏和情绪作用",
  "scope": "general_candidate",
  "suggested_change": "判断停顿时增加对前后语义和强调关系的检查"
}
```

## 6. 反馈适用范围

每条反馈必须归入以下一种范围：

- `single_case`：只适用于当前视频，不修改规则。
- `creator_preference`：属于某位创作者的个人习惯。
- `content_type`：适用于某类视频，例如知识口播或短视频带货。
- `general_candidate`：可能适用于大部分单人口播，进入通用规则候选池。

## 7. SOP 更新条件

不得因为单个案例直接修改通用 SOP。满足以下条件后才考虑更新：

1. 相同问题在多个独立素材中重复出现。
2. 问题能够总结成清晰、可执行的判断规则。
3. 新规则不会明显增加其他类别的误删风险。
4. 人工确认该规则具有通用性。
5. 使用回归案例验证旧能力没有退化。

## 8. 版本管理

- 文案澄清但不改变行为：补丁版本，例如 `v0.1.1`。
- 新增或调整判断规则：次版本，例如 `v0.2`。
- 修改接口或整体决策方式：主版本，例如 `v1.0`。

每次更新必须记录：

- 修改原因。
- 依据的反馈案例。
- 新增、删除或调整的规则。
- 回归测试结果。
- 已知限制。

## 9. 第一阶段建议验收门槛

在小规模真实口播测试集中，建议先达到：

- 不出现改变原意的自动删除。
- 明确口误重说能够稳定识别。
- 自动删除以高置信度项目为主。
- 所有低置信度判断进入人工复核。
- 转换到 FCP7 XML 后，已确认边界能够逐帧一致。

达到上述条件后，再逐步开放语气词、停顿和跑题内容的自动处理。

